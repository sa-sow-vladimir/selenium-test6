webdriver {
  driver = remote
  remote {
        url="http://localhost:4444/wd/hub"
        driver=chrome
    }
}

headless.mode = false

serenity{
    take.screenshots = AFTER_EACH_STEP
    console{
        colors = true
        timestamp = true
    }
    logging = verbose
}

serenity.browser.maximized = true

